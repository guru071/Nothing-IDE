# UUID Generator

Adds a single command to the command palette: **UUID Generator: Copy a new UUID v4**.

Running it generates a random UUID v4 (via `crypto.randomUUID()`), copies it to your
clipboard, and shows it in a toast.
