# JSON Flatten

Flattens a nested JSON object/array into a single-level object with
dot-notation keys (`a.b.0.c`), or reverses that back into nested JSON.

## Commands

- **JSON: Flatten to Dot Notation**
- **JSON: Unflatten from Dot Notation**
