# URL Tools

Encode or decode the selected text as a URL component.

## Commands

- **URL: Encode Selection** — applies `encodeURIComponent` to each selection.
- **URL: Decode Selection** — applies `decodeURIComponent` to each selection.
