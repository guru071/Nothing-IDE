# Number Base Converter

Converts a selected integer between decimal, hex (`0x`), binary (`0b`),
and octal (`0o`) - auto-detects the source base from its prefix.

## Commands

- **Number: Convert to Hex**
- **Number: Convert to Binary**
- **Number: Convert to Octal**
- **Number: Convert to Decimal**
