# Hash Generator

Computes a cryptographic hash of the current selection (or the whole file
if nothing is selected) and copies it to the clipboard.

## Commands

- **Hash: Copy SHA-256 of Selection**
- **Hash: Copy SHA-1 of Selection**
