# JSON Formatter

Adds two commands to the command palette:

- **JSON: Format (Pretty Print)** - pretty-prints the selected JSON (or the
  whole file, if nothing is selected) with 2-space indentation
- **JSON: Minify** - collapses it back to a single line

Shows a toast with the parse error if the text isn't valid JSON.
