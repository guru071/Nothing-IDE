# CSS Unit Converter

Converts a selected CSS length between `px` and `rem`, assuming the
standard 16px base font size.

## Commands

- **CSS: px to rem (base 16px)**
- **CSS: rem to px (base 16px)**
