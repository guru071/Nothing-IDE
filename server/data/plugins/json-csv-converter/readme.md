# JSON/CSV Converter

Converts a selected JSON array of flat objects to CSV, or a selected CSV
table back to a JSON array (all values come back as strings, same as any
CSV round-trip - CSV has no type system).

## Commands

- **JSON/CSV: Convert JSON Array to CSV**
- **JSON/CSV: Convert CSV to JSON Array**
