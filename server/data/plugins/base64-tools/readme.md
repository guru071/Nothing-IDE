# Base64 Tools

Adds two commands to the command palette:

- **Base64: Encode Selection** - encodes the current selection(s) as Base64
- **Base64: Decode Selection** - decodes Base64 text in the current selection(s)

Works with multiple cursors/selections at once.
