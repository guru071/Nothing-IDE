# SQL IN Clause

Converts selected lines into a SQL `IN (...)` list.

## Commands

- **SQL: Lines to IN Clause (quoted strings)** - wraps each value in
  single quotes, escaping embedded quotes.
- **SQL: Lines to IN Clause (numbers)** - no quoting.
