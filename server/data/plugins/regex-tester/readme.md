# Regex Tester

Tests a regex pattern against text, right in the editor - no external
tool needed.

Select text where the **first line** is a `/pattern/flags` regex literal
and the remaining lines are the text to test against, then run the
command. It appends a match report (count, matched text, index, and any
capture groups) below your selection.

## Commands

- **Regex: Test Pattern**
